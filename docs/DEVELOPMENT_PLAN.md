# Development Plan — Rookie to CEO (Unity 2D)

기획 원문은 [GDD.md](./GDD.md) 참고. 이 문서는 개발 로드맵과, GDD에서 아직 확정되지 않은 밸런스 수치를 추적한다.

## 기술 스택

- Engine: Unity 6000.x LTS (미설치 시 2022.3 LTS로 대체)
- Render Pipeline: URP 2D (2D 탑뷰 스프라이트)
- Input: Unity Input System (WASD + Space/R/E/Shift 고정 바인딩, 마우스 조준 없음)
- Test: Unity Test Framework (EditMode 위주 — 순수 로직을 MonoBehaviour와 분리해 배치모드에서 검증)
- 데이터: 밸런스 수치는 ScriptableObject로 관리해 코드 수정 없이 튜닝 가능하게 함

## 아키텍처 개요

- 게임플레이 로직(스탯 계산, 웨이브 타이머, 쿨타임, 시야 판정 등)은 MonoBehaviour와 최대한 분리된 순수 C# 클래스로 작성 → EditMode 테스트로 CLI에서 검증 가능
- 씬/프리팹 구성은 `Assets/Editor/`의 자동화 스크립트가 코드로 생성 (`-batchmode -executeMethod`) → GUI 드래그 없이 재현 가능
- 밸런스 수치는 `Assets/ScriptableObjects/`의 데이터 애셋으로 분리

## 아트 파이프라인

- **스타일**: 도트(픽셀 아트). 사무용품 소재(이메일 봉투, 스테이플러, 서류 등)와 탑뷰 서바이버형 디펜스 장르에 표준적으로 잘 맞는 선택.
- **해상도 기준**: 캐릭터/적 32x32 그리드, 바닥 타일 32x32, PPU(Pixels Per Unit) 32로 통일 — 스프라이트 크기가 섞이면 콜라이더/스케일을 개별 보정해야 해서 초반에 고정.
- **Unity 임포트 설정**: Sprite Mode = Single/Multiple(시트인 경우), Filter Mode = Point(no filter), Compression = None, Generate Mip Maps = Off (2D 탑뷰라 밉맵 불필요, 블러 방지).
- **제작 툴**: Aseprite(유료, 인디 픽셀아트 사실상 표준 — 스프라이트시트 export가 Unity 임포트와 궁합 좋음) 또는 Piskel(무료 대안).
- **AI 생성 보조**: nano-banana 스킬로 픽셀아트풍 초안 생성 가능하나, AI 출력은 픽셀 그리드가 정확히 안 맞으므로 Aseprite/Piskel에서 그리드 정렬·투명배경 정리 후 사용.
- **애니메이션 프레임 최소화 (MVP 범위)**: idle 1프레임, walk 4프레임, attack 2~3프레임 기준 — 이 이상은 M9 이후 폴리싱 단계에서 검토.
- **제작 순서**: M2~M8은 단색 사각형/원(프로그래머 아트)으로 게임플레이 로직을 먼저 검증 → M9(밸런싱 확정) 전후로 실제 도트 스프라이트로 교체. 스프라이트 교체 시 콜라이더 크기/애니메이터 파라미터 재조정 필요할 수 있음.

## 로드맵 (마일스톤은 [CHECKPOINT.md](../CHECKPOINT.md)에서 상태 추적)

| # | 마일스톤 | 핵심 산출물 |
|---|---|---|
| M0 | 문서/리포 초기화 | docs/, CHECKPOINT.md, AUDIT.log, git init |
| M1 | Unity 설치 | Unity Hub + Editor (LTS) |
| M2 | 프로젝트 생성 & 자동화 파이프라인 | RookieToCEO/ 프로젝트, Editor 스크립트 골격 |
| M3 | 코어 시스템 | PlayerController, StatSystem, ReputationManager |
| M4 | 무기 시스템 | 키보드 샷건, 스테이플러 연사, 업무 떠넘기기, 퇴사 통보 |
| M5 | 적 & 스폰 | EnemyBase + 5종, SpawnManager |
| M6 | 낮 디펜스 루프 | DayWaveManager, 레벨업 UI, 상사의 눈치 |
| M7 | 밤 잠입 | NightManager, 시야 판정, 조사/탈출 |
| M8 | CEO 웨이브 & 엔딩 | BossWaveManager, 엔딩 트리거 |
| M9 | 밸런싱 확정 & 통합 검증 | 아래 밸런스 표 수치 확정, 전체 테스트/빌드 통과 |

## 밸런스 표 (M9 확정치)

수치는 GDD 정성적 설명("느림", "빠름", "높은 HP" 등)을 기반으로 M9에서 확정했다. 실제 값의
단일 소스는 `Assets/ScriptableObjects/BalanceData.asset`이며, 이 표는 그 값을 그대로 옮겨
적은 것이다 — 애셋을 바꾸면 이 표도 같이 갱신한다. 실제 플레이테스트로 조정이 필요해지면
코드 재컴파일 없이 이 애셋만 수정하면 된다.

### 무기 수치

| 무기 | 데미지 | 공격 간격 | 범위/각도 | 비고 |
|---|---|---|---|---|
| 키보드 샷건 | 12 | 1.2초 (느림) | 사거리 3, 부채꼴 절반각 60°, 최대 5타겟 | 시작 무기 |
| 스테이플러 연사 | 6 | 0.3초 (빠름) | 사거리 6, 절반각 8°(직선형), 관통 1+레벨 | 1층 밤 보상, 레벨당 공속 +15%. 무기 레벨은 플레이어 레벨업(3택1)에 맞춰 함께 오름 |
| 업무 떠넘기기 | 넉백력 8 | 쿨타임 12초(고정) × 짬 배율 | 반경 2.5 | 액티브(Space) |
| 퇴사 통보 | 공포 4초 / 슬로우 4초(50%) | 게이지 100, 처치당 +10(10킬 충전) | 화면 전체(일반)/정예/보스 3초 정지 | 궁극기(R) |

### 적 수치

| 적 | HP | 이동속도 | 접촉 데미지 | 특수 행동 |
|---|---|---|---|---|
| 이메일 봉투 | 20 | 1.5 (보통) | 10 | 직선 이동 |
| 서류 더미 | 40 | 0.8 (느림) | 10 | 탱커 |
| 긴급 수정 포스트잇 | 15 | 1(평시 0.3배)/돌진 시 3배 | 12 | 정지 1초→돌진 0.4초→재정비 1.2초 순환 |
| 회의 요청 달력 | 25 | 0.9 | 8 | 반경 2 안에서 플레이어 공격속도 50% 감소(0.5초 단위 갱신) |
| 클레임 전화기 | 20 | 1 | 8 (원거리) | 선호 사거리 4 유지, 사거리 5 안에서 2초마다 공격 |
| CEO 최종 지시서 | - (무적) | 0(고정) | - | 5초마다 반경 3 안에 일반 업무 소환, 퇴사통보로 3초 정지 |

### 층별 적 생성량 (스폰 테이블)

| 층 | 기준 스폰 간격 | 0~15초 | 15~30초 | 30~45초 | 45~60초 |
|---|---|---|---|---|---|
| 1층 | 2.5초 | 이메일 봉투 | +서류 더미 | 생성속도 ×1.5(30초부터 공통 적용) | - |
| 2층 | 2.0초 | 이메일+서류 | +포스트잇 | 생성속도 ×1.5 | - |
| 3층 | 1.5초 | 기존 전체 | +회의요청/클레임전화 | 생성속도 ×1.5 | - |
| 4층(보스) | 1.2초 | 업무 폭탄(기존 전체) | +포스트잇/회의요청, 상사의 시선 강제 발동 1회 | - | CEO 최종 지시서 등장 + 5초마다 빨간 구역(반경 1~6, 초당 5 피해, 4초 지속) |

기준 스폰 간격은 `WaveSpawnTable.GetBaseSpawnIntervalSeconds(floor)`가 결정하고, 여기에
시간대별 생성속도 배율을 곱해 최종 스폰 간격을 계산한다(`SpawnManager`). 층이 올라갈수록
간격이 짧아져(=더 자주 스폰) GDD 5번의 "1층 적음 → 3층 크게 증가" 난이도 곡선을 반영한다.
M9 초반에는 모든 층이 2초 고정이라 이 곡선이 전혀 반영되지 않았던 걸 뒤늦게 발견해 수정했다.
초당 개체 수를 더 세분화하는 튜닝은 실제 플레이테스트 이후 이 함수 값만 조정하면 된다.

### 레벨업 강화 수치

| 스탯 | 강화당 효과 | 비고 |
|---|---|---|
| 업무처리력 | 공격력 +10% | 모든 무기 (곱연산 누적) |
| 손속도 | 공격속도 +10% | 기본 무기 |
| 눈치 | 이동속도 +8% | |
| 멘탈 관리 | 최대HP +20 | 즉시 최대치만큼 채워지진 않고 현재 HP는 유지, 최대치만 증가 |
| 일머리 | 공격 범위 +10% | |
| 짬 | 액티브 쿨타임 -8% (최소 배율 0.1) | |

경험치 곡선: 레벨업에 필요한 경험치는 `50 + (레벨-1)×20`이고, 적 처치당 경험치 10을 얻는다
(`LevelSystem`/`EnemyBase.Die()`). 층당 2~3회, 전체 8~10회 레벨업이 되도록 GDD 4번 기준에
맞춰 조정한 값이며, 실제 플레이 곡선은 이후 플레이테스트로 재조정할 수 있다.

## 검증 전략

- 순수 로직(스탯, 쿨타임, 웨이브 타이머, 시야 판정, 게이지)은 EditMode 유닛 테스트로 커버
- 씬 구성은 Editor 자동화 스크립트 실행 로그로 무인 검증
- 최종 빌드는 배치모드 빌드 성공 여부로 검증
